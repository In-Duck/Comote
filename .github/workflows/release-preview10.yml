name: Release Preview 12

on:
  push:
    branches:
      - main
      - agent/complete-comote-prototype

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Build manager and client
        shell: pwsh
        run: ./Distribution/Build_Hub_Packages.ps1

      - name: Create separate download packages
        shell: pwsh
        run: |
          Compress-Archive `
            -Path artifacts/Comote-1.6.0-preview.12/Manager/* `
            -DestinationPath artifacts/ComoteManager-1.6.0-preview.12-win-x64.zip `
            -CompressionLevel Optimal
          Compress-Archive `
            -Path artifacts/Comote-1.6.0-preview.12/Client/* `
            -DestinationPath artifacts/ComoteClient-1.6.0-preview.12-win-x64.zip `
            -CompressionLevel Optimal

      - name: Publish GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $assets = @(
            "artifacts/ComoteManager-1.6.0-preview.12-win-x64.zip",
            "artifacts/ComoteClient-1.6.0-preview.12-win-x64.zip",
            "artifacts/Comote-1.6.0-preview.12-win-x64.zip"
          )

          if (gh release view "v1.6.0-preview.12" 2>$null) {
            gh release upload "v1.6.0-preview.12" $assets --clobber
          } else {
            gh release create "v1.6.0-preview.12" $assets `
              --target "${{ github.sha }}" `
              --title "Comote 1.6.0 Preview 12" `
              --notes "Adds selectable virtual HID keyboard/mouse input through the signed FakerInput driver, includes the verified driver installer in the Client package, and keeps SendInput as a compatibility mode."
          }